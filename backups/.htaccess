# 🔒 Security: Block direct HTTP access to backup archives
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>

# Optional: Prevent directory listing just in case
Options -Indexes
